@import "./config.styl"
@import "./text.styl"

tag()
  box-shadow: inset 0 0 0 1px currentColor
  textCaps()
  color: $colorPrimary
  border-radius: 99rem
  display: inline-block
  font-size: 0.75rem
  padding: 0.5rem 0.75rem
  font-weight: 600
  line-height: 1

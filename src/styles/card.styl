@require "./config.styl"

card()
  background: #fff
  border-radius: 4px
  padding: $spacingBase;
  // border-radius: 2px
  // box-shadow: rgba(0,0,0,.1) 0 1px 2px
  // box-shadow: rgba(0, 0, 0, 0.1) 0 2px 5px //, rgba(0, 0, 0, 0.1) 0 1px 1px;
